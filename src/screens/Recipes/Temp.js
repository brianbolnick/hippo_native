import React from "react";
import { Text } from "react-native";

export default ({ navigation }) => (
  <Text
    style={{
      flex: 1,
      justifyContent: "center",
      alignItems: "center",
      paddingTop: 200
    }}
  >
    COMING SOON
  </Text>
);
